---
import ArrowLeft from '@icons/ArrowLeft.astro'

interface Props {
  title: string
  numQuestions: number
  image: string
}

const { title, numQuestions, image } = Astro.props
---

<header class="flex items-center gap-5 text-xl text-gray-400">
  <a href="/" class="transition-transform hover:scale-125 hover:text-sky-blue">
    <ArrowLeft class="w-8 h-8" />
  </a>

  <div class="flex items-center flex-grow gap-2">
    <img src={image} class="w-8" alt="Logo de tecnología" />
    <h1>{title}</h1>
  </div>

  <p>
    <span id="quiz-counter">1</span>/{numQuestions}
  </p>
</header>
